#ifndef CUSTOMISED_SERVER_H
#define CUSTOMISED_SERVER_H

#include <DNSServer.h>
#include <ESPAsyncWebServer.h>

extern DNSServer dnsServer;

class CustomisedServer {
public:
    CustomisedServer();
};

#endif